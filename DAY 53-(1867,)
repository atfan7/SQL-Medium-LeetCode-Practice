PROBLEM 1867 :::

with cte as (
select 
order_id,
max(quantity) as max_quan,
avg(quantity) as avg_quan

from 
OrdersDetails
group by order_id 
)

select
order_id

from 
cte 
where max_quan >(select max(avg_quan) from cte)
